# Non-torch dependencies for genre_gender_tagger.py
#
# PyTorch + torchaudio are NOT listed here. They are installed
# separately with the correct CUDA / CPU index URL. See readme.md
# or run install-deps.bat, which handles both for you.

soundfile
pandas
tqdm
mutagen
transformers

# Acapella / voice-gender path (Essentia gender-discogs-effnet via TF).
# Essentia itself has no Windows wheels; we run the official .pb
# models with TensorFlow + a MusiCNN-style mel front-end.
tensorflow>=2.15,<2.17
librosa
soxr
