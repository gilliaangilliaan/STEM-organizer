# Build / packaging
.build-venv/
build/
dist/
build.log
build_assets/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/

# One-time / runtime installs (install-deps.bat)
site-packages/
ffmpeg/
torch_home/
python-version.txt

# Local app state (created beside exe or when running from source)
settings.json

# Scratch / experiments
.tmp-sf/

# Design sources (bundled assets are logo.ico / logo.png)
*.psd
