PySide6>=6.6,<6.9
PySide6-Fluent-Widgets>=1.11,<2
# ML / audio backends (same as original STEM organizer)
# torch is installed separately by install-deps.bat (CUDA 12.4 / 12.8 / CPU)
numpy
scipy
soundfile
demucs
sounddevice
psutil
requests
# Optional tag writers / metadata
mutagen
# Overview compression detect (spectral fake-lossless)
flac-detective==1.7.0
# PIL/Pillow has no direct import in this app's source — it is a transitive
# dependency of torchvision (used by hear21passt/timm in the tagger venv).
# Listed here only so the frozen --no-deps site-packages install includes it.
Pillow
